#include <iostream>
#include <vector>
#include <list>

using namespace std;

/*
* (std::list) лист - это реализация двухсвязного списка
* (std::forward_list) - это реализация односвязного списка
*/

int main() 
{
    setlocale(LC_ALL, "ru");
 
    
  
    return 0;
}